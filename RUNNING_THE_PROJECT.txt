SPATIAL GESTURE CONTROL - RUNNING THE PROJECT
================================================

This guide covers development and local builds on Windows, macOS, and Linux.
Run all project commands from the repository root unless stated otherwise.

Repository:
https://github.com/meanOtaku/gesture-controls

Official Tauri prerequisite reference:
https://v2.tauri.app/start/prerequisites/


1. COMMON REQUIREMENTS
----------------------

All operating systems require:

- Git
- Node.js 20 or newer (Node.js 22 LTS is recommended)
- npm
- The current stable Rust toolchain installed through rustup
- A graphical desktop session when launching the Tauri window

Confirm the tools are available:

  git --version
  node --version
  npm --version
  rustc --version
  cargo --version

If Rust is not installed:

Linux/macOS:

  curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh

Windows PowerShell:

  winget install --id Rustlang.Rustup

Restart the terminal after installing Node.js, Rust, or system build tools.


2. GET THE SOURCE AND INSTALL JAVASCRIPT DEPENDENCIES
-----------------------------------------------------

  git clone https://github.com/meanOtaku/gesture-controls.git
  cd gesture-controls
  npm install

For an existing checkout, use the reproducible lockfile installation:

  git pull
  npm ci


3. LINUX SETUP
--------------

Debian / Ubuntu:

  sudo apt update
  sudo apt install -y \
    libwebkit2gtk-4.1-dev \
    build-essential \
    curl \
    wget \
    file \
    libxdo-dev \
    libssl-dev \
    libayatana-appindicator3-dev \
    librsvg2-dev \
    patchelf \
    pkg-config

Fedora:

  sudo dnf check-update
  sudo dnf install -y \
    webkit2gtk4.1-devel \
    openssl-devel \
    curl \
    wget \
    file \
    libappindicator-gtk3-devel \
    librsvg2-devel \
    libxdo-devel
  sudo dnf group install "c-development"

Arch Linux:

  sudo pacman -Syu
  sudo pacman -S --needed \
    webkit2gtk-4.1 \
    base-devel \
    curl \
    wget \
    file \
    openssl \
    appmenu-gtk-module \
    libappindicator-gtk3 \
    librsvg \
    xdotool

Start the development application:

  npm run tauri dev

Build installable Linux packages:

  npm run tauri build

Linux notes:

- The Tauri window cannot open from a headless SSH-only terminal without a
  graphical X11 or Wayland session.
- If pkg-config reports that webkit2gtk-4.1, librsvg, or appindicator is
  missing, install the corresponding development package for your distribution.


4. macOS SETUP
--------------

For built-in Sony tracking, use macOS 14 or newer. Install the Xcode Command Line Tools:

  xcode-select --install

Alternatively, install full Xcode and launch it once to finish setup.

Start the development application:

  npm run tauri dev

Build the macOS application and disk image:

  npm run tauri build

macOS notes:

- The first build can take several minutes while Rust compiles dependencies.
- macOS may warn about unsigned local builds. Code signing and notarization are
  separate release-distribution steps and are not needed for normal development.
- Apple Silicon and Intel Macs use the same project commands; build on the target
  architecture unless you intentionally configure cross-compilation.


5. WINDOWS SETUP
----------------

Install these Windows dependencies:

1. Microsoft C++ Build Tools:
   https://visualstudio.microsoft.com/visual-cpp-build-tools/

   In the installer, select "Desktop development with C++".

2. Microsoft Edge WebView2 Evergreen Runtime:
   https://developer.microsoft.com/en-us/microsoft-edge/webview2/

3. Rust through rustup, if it is not already installed:

   winget install --id Rustlang.Rustup

Open a new PowerShell terminal after installation.

Clone and install, if not already done:

  git clone https://github.com/meanOtaku/gesture-controls.git
  cd gesture-controls
  npm install

Start the development application:

  npm run tauri dev

Build the Windows executable and installers:

  npm run tauri build

Windows notes:

- If MSI creation fails while running light.exe, enable the Windows VBSCRIPT
  optional feature under Settings -> Apps -> Optional features -> More Windows
  features, then restart Windows if prompted.
- WebView2 is included with many current Windows installations, but installing
  the Evergreen Runtime explicitly avoids missing-runtime failures.


6. USE THE BUILT-IN SONY HEAD TRACKER
-------------------------------------

The production tracker is part of the Tauri process. Do not install or launch the
upstream sony-head-tracker GUI, bridge, CLI, or a sidecar.

Windows 11:

1. Pair and connect the Sony headset in Windows Bluetooth settings.
2. Start this Tauri application.
3. The built-in engine first tries descriptor-verified HID access and can fall
   back to the Windows Sensor API.
4. Use the Recenter button while facing forward.

macOS 14 or newer:

1. Pair and connect the Sony headset in System Settings -> Bluetooth.
2. Start this Tauri application.
3. If macOS requests Input Monitoring access, grant it to this Tauri app and
   restart the app. The tracker code runs in-process, so there is no second
   executable to authorize.
4. Use the Recenter button while facing forward.

Linux:

The desktop application builds and runs, but direct Sony hardware acquisition is
reported as unsupported until a Linux backend has been validated with physical
hardware. Use the compatibility simulator below when developing on Linux.

COMPATIBILITY SIMULATOR

The former JSON UDP receiver is disabled by default. Select it explicitly before
starting Tauri; it binds only to 127.0.0.1:4243.

Linux/macOS terminal 1:

  SGC_HEAD_TRACKER_SOURCE=udp npm run tauri dev

Linux/macOS terminal 2:

  python3 head-tracking/scripts/send_sample.py

Windows PowerShell terminal 1:

  $env:SGC_HEAD_TRACKER_SOURCE = "udp"
  npm run tauri dev

Windows PowerShell terminal 2:

  py head-tracking\scripts\send_sample.py

The sample sends one packet, so the dashboard briefly connects and returns to its
waiting state. Run it repeatedly for additional samples.

If direct tracking never connects:

- Confirm the headset is paired, connected, and supports Android Head Tracker HID.
- On macOS, verify Input Monitoring permission for this application.
- Check the Tauri terminal and the built-in runtime status shown in the dashboard.
- Disconnect other applications that may have exclusive access to the sensor.
- Use the compatibility simulator to distinguish UI/event problems from native
  device-discovery problems.


7. RUN TESTS AND QUALITY CHECKS
-------------------------------

Frontend tests plus Tauri icon/config validation:

  npm test

TypeScript checking:

  npm run typecheck

Production frontend build:

  npm run build

Rust protocol, direct native provider, and UDP compatibility tests:

  cargo test --workspace --all-targets

Rust formatting check:

  cargo fmt --all -- --check

Rust linting:

  cargo clippy --workspace --all-targets --all-features -- -D warnings

Existing Python interoperability tests (requires uv):

  uv run --directory head-tracking --with pytest pytest -q


8. BUILD OUTPUT
---------------

After this command:

  npm run tauri build

Tauri prints the exact generated artifact paths. Build products are normally
placed under:

  target/release/bundle/

The platform-specific folders may contain Linux packages, a macOS app/DMG, or
Windows MSI/NSIS installers depending on the host OS.

Native desktop installers should normally be built on their target operating
system. The GitHub Actions workflow also builds the project on Ubuntu, macOS,
and Windows to catch platform-specific failures.


9. FRONTEND-ONLY PREVIEW
------------------------

To preview only the React dashboard without compiling or launching Tauri:

  npm run dev --workspace @spatial-gesture/desktop

Then open:

  http://127.0.0.1:1420

This mode is useful for UI development, but it does not start the built-in Sony
tracking engine or expose native Tauri functionality. Use "npm run tauri dev"
for the complete application.
